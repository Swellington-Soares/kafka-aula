spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aulakafka
    username: devuser
    password: dev1234
  application:
    name: pixconsumidor
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
